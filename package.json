{
  "name": "ai-kb",
  "version": "0.0.0",
  "private": true,
  "description": "Karpathy-style cert-prep KB + gamified dungeon (Slay the Cert)",
  "scripts": {
    "lint": "biome check --diagnostic-level=error .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "cd public/dungeon && npm test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
