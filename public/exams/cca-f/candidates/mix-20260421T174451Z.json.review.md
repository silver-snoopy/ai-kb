# Review — mix-20260421T174451Z.json

Generated: 2026-04-21T17:51:24.270848+00:00
Candidate: `public/exams/cca-f/candidates/mix-20260421T174451Z.json` (165 questions)

## Decision: **BLOCKED**

Per `/cca-f-verify-questions` invariant, any CRITICAL or HIGH flag blocks merge. Candidate remains in `candidates/`; bank.json unchanged.

## Flag summary

| Severity | Flags | Unique questions |
| --- | --- | --- |
| CRITICAL | 4 | 4 |
| HIGH     | 6 | 6 |
| MEDIUM   | 5 | 5 |
| LOW      | 2 | 2 |

## Inline checks (pre-reviewer)

- ID collisions vs bank: **0** (pass)
- Duplicate ids within candidate: **0** (pass)
- Stale-term hits (CLAUDE.md list): **0** (pass)
- Hard-constraint `domain ∈ bank.scenarios[S].domains` violations: **0** (pass)

## Per-reviewer totals

| Reviewer | CRITICAL | HIGH | MEDIUM | LOW | Total |
| --- | --- | --- | --- | --- | --- |
| R1-factcheck | 1 | 0 | 0 | 2 | 3 |
| R2-distractor | 3 | 6 | 5 | 0 | 14 |

## CRITICAL flags

### `mix-733` (domain=domain-1-agentic, scenario=3, marked-correct=B)

> After the web search and document analysis subagents complete their tasks, the coordinator needs to spawn the synthesis subagent to synthesize the findings. What is the correct approach for providing the synthesis subagent with the information it needs?

- **[R2-distractor]**  — Option A (including findings directly in prompt) appears equally valid as B (reference identifiers + shared memory); both are reasonable context-passing approaches.
  - {'bad_option': 'A'}

### `mix-782` (domain=domain-4-mcp, scenario=4, marked-correct=A)

> You've configured your Claude agent with three MCP servers: one for git operations, one for Jira ticket management, and one for documentation search. When a user asks the agent to "create a branch for JIRA-123 and add documentation links to the ticket," how do…

- **[R2-distractor]**  — Options A and B both describe valid aspects of MCP integration; the distinction between 'discovered at connection time' vs 'sequentially queried' is subtle and not clearly wrong.
  - {'bad_option': 'B'}

### `mix-790` (domain=domain-4-mcp, scenario=1, marked-correct=A)

> Your expense reimbursement agent processes employee requests using a `process_reimbursement` tool. Company policy requires that reimbursements above $500 must be approved by a manager before funds are disbursed. The agent handles hundreds of requests daily, an…

- **[R1-factcheck]**  — _quick-reference.md and Scenario 1 mandate hooks (PreToolUse/PostToolUse) for compliance enforcement, not prompt-based or no-enforcement approaches.
  - {'marked_correct': 'A', 'substrate_correct': 'D'}

### `mix-816` (domain=domain-4-mcp, scenario=4, marked-correct=A)

> Your marketing agent connects to MCP servers from three different vendors: an email service, a social media platform, and an analytics dashboard. Each server provides tool annotations—some tools marked with `readOnlyHint: true`, others with `destructiveHint: t…

- **[R2-distractor]**  — Option C is validated as a sound approach ('valid but doesn't scale'), making the question ambiguous—two defensible answers.
  - {'bad_option': 'C'}

## HIGH flags

### `mix-758` (domain=domain-2-claude-code, scenario=2, marked-correct=D)

> A critical bug is affecting production users. Error logs show exceptions in the OrderProcessing module with a clear stack trace pointing to a specific function. You haven't worked with this module before. What's the most effective approach?

- **[R2-distractor]**  — Explanation contradicts itself: both A and C appear correct based on the rationale, creating ambiguity about what constitutes correct reasoning for unfamiliar code.
  - {'bad_option': 'A'}

### `mix-764` (domain=domain-2-claude-code, scenario=2, marked-correct=A)

> Your monorepo contains shared coding standards in `/docs/standards/`: `security-rules.md` (for services handling user data), `testing-patterns.md` (for all packages), and `api-conventions.md` (for API-facing services). Your 15 packages are organized by feature…

- **[R2-distractor]**  — @imports syntax is dismissed as non-standard but lacks grounding in a clear anti-pattern from substrate; rationale is weak.
  - {'bad_option': 'B'}

### `mix-858` (domain=domain-3-prompt-engineering, scenario=6, marked-correct=B)

> After implementing tool use with strict schema definitions, JSON syntax errors are eliminated, but 5% of extractions still have valid JSON with empty arrays or null values for required fields like citations and methodology. Spot-checking reveals that source do…

- **[R2-distractor]**  — Distractor recommends blind retry without error feedback—not mechanistically sound; lacks signal for what to fix.
  - {'bad_option': 'A'}

### `mix-867` (domain=domain-3-prompt-engineering, scenario=6, marked-correct=A)

> Your invoice extraction uses tool use with strict JSON schemas. JSON syntax errors never occur, but 12% of extractions fail semantic validation—for example, line item amounts don't sum to the extracted total, or vendor IDs don't match valid formats. These fail…

- **[R2-distractor]**  — Distractor suggests blind retries without error feedback—repetition without signal won't improve extraction on second attempt.
  - {'bad_option': 'C'}

### `mix-872` (domain=domain-5-context, scenario=1, marked-correct=B)

> You're implementing a feature where users refine their playlist preferences through multiple conversation turns. After deploying, you notice Claude's responses don't reflect what users said earlier in the same conversation—for example, a user says they love ja…

- **[R2-distractor]**  — Distractor proposes non-existent Claude API feature ('vector database connection'); trivially false premise.
  - {'bad_option': 'D'}

### `mix-900` (domain=domain-3-prompt-engineering, scenario=2, marked-correct=B)

> Your fitness coaching assistant uses a system prompt with detailed conditional logic: "If the user mentions being a beginner, provide step-by-step form instructions. If they use terms like 'progressive overload' or 'superset', respond concisely. If they ask ab…

- **[R2-distractor]**  — Distractor ('add more conditional branches') directly instantiates domain-3 anti-pattern 'vague conditional logic'; explanation confirms it doesn't help.
  - {'bad_option': 'A'}

## MEDIUM flags

- `mix-740` — **[R2-distractor]** Synthesis agent direct access to tools violates coordination but is not explicitly named as anti-pattern in substrate with same force as others.
- `mix-743` — **[R2-distractor]** Context window space as secondary concern is not clearly rooted in a named anti-pattern; tool selection accuracy is the primary failure mode.
- `mix-853` — **[R2-distractor]** Post-processing distractor is plausible but doesn't match schema-design anti-pattern; weak without grounding in core design principle.
- `mix-862` — **[R2-distractor]** Post-extraction flagging is reactive approach; plausible but doesn't address schema-design root cause (multi-value capture).
- `mix-899` — **[R2-distractor]** Post-extraction taxonomy normalization is plausible but reactive; distractor lacks grounding in few-shot learning anti-pattern.

## LOW flags

- `mix-609` — **[R1-factcheck]** Question tests --resume flag for Claude Code session resumption; flag documented in domain-1-agentic context but not in domain-2-claude-code substrate.
- `mix-680` — **[R1-factcheck]** Question tests partial assistant message seeding to control response openings; technique not documented in provided substrate materials.

## Next steps

- Candidate is BLOCKED on 4 CRITICAL + 6 HIGH flags.
- Fix each CRITICAL/HIGH flagged question in-place in the candidate JSON (re-pick the correct answer, rewrite distractors, or amend explanation), then re-run `/cca-f-verify-questions`.
- MEDIUM and LOW flags do not block but are worth reviewing; most are weak-plausibility distractors or unverifiable topics outside the substrate.

---

## Round 2 — Post-fix re-verification

Fixes applied (see commit `c83880b`): 2 correct-answer flips (mix-733 B→A, mix-790 A→D) + 8 explanation rewrites (mix-782/816/758/764/858/867/872/900).

Post-fix re-reviewer checked all 10 against the substrate for (a) fact-check, (b) distractor audit, (c) explanation audit in one pass.

**Result: 10/10 RESOLVED, 0 CRITICAL, 0 HIGH.**

Merged to bank: `v1 → v2`, total `364 → 529`, 165 questions appended (all `source: "mix"`).
